<%= simple_form_for @lesson do |f| %>
  <%= f.input :title %>
  <%= f.input :description, as: :text %>
  <%= f.button :submit %>
<% end %>

<%= link_to "Cancel", user_lessons_path(current_user), class: 'button' %>
