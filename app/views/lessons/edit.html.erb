<h1>Editing lesson</h1>

<%= "Editing #{@lesson.title}" %>

<%= simple_form_for @lesson do |f| %>
  <%= f.input :title %>
  <%= f.input :description, as: :text %>
  <%= f.submit 'Confirm edit' %>
<% end %>

<%= link_to 'Back', lesson_path %>
